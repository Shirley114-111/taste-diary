/* =========================================================
   Taste Diary · 前端运行时配置
   ---------------------------------------------------------
   API_BASE = 后端服务的网址。

   1) 本地开发（后端自己托管前端，同一个地址）：
        留空字符串 ''  即可，前端会用相对路径访问 /api/...

   2) 前后端分开部署（前端在 Vercel，后端在 Render）：
        把下面改成你的后端网址，例如：
        window.API_BASE = 'https://stardew-meal-backend.onrender.com';
        注意：结尾不要加斜杠 /
   ========================================================= */
window.API_BASE = 'https://taste-diary.onrender.com';
